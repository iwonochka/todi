<div class="main-container">

<%= render 'components/searchbar' %>


  <div class="map-container">
    <div class="map" style="width: 100%; height: 600px;"
      data-controller="mapbox"
      data-mapbox-markers-value="<%= @markers.to_json %>"
      data-mapbox-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
    </div>
  </div>

  <div class="orange-buttons-container">
  </div>

  <%# <div class="profiles-card-container"> %>
    <%# <div class="profiles-card">
      <img src="" />
      <div class="profiles-card-infos">
        <h2>user.name</h2>
        <p>distance from user in m or km</p>
      </div>
    </div> %>
  <%# </div> %>

</div>

<%= render 'components/profile-mini-card' %>

<%# All elements needed: %>
<%# main container?? %>
<%# map -> map-container %>
<%# orange buttons -> orange-buttons-container %>
<%# cards -> profiles-cards-container %>
<%# navbar %>
